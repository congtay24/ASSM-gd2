<h3 class="text-center">Danh sách quần áo</h3>

<a href="./add" class="btn-primary btn btn-block">Thêm</a>
<a href="./" class="btn-primary btn btn-block float-right" style="float: right;">Đăng xuất</a>
<table class="table col-md-4 bg-light p-3, text-center">
  <tr>
    <th>Mã sản phẩm</th>
    <th>Tên sản phẩm</th>
    <th>Giá sản phẩm</th>
    <th>Ảnh</th>
    <th>Màu sản phẩm</th>
    <th></th>
    <th></th>
  </tr>

  {{!-- {{#each }} {{/each}} --}}
  {{!-- là một hướng dẫn xử lý vòng lặp trong handlebars, --}}
  {{!-- nó cho phép lặp lại một mảng hoặc đối tượng và tạo đầu ra HTML cho mỗi phần tử --}}

  {{#each users}}
  <tr>
    <td>{{maSP}}</td>
    <td>{{tenSP}}</td>
    <td>{{giaSP}}  <p style="color: red;">VNĐ</p></td>
    <td><img style="width: 100px" src="{{anh}}"></td>
    <td>{{mauSP}}</td>
    <td><a class="btn-primary btn btn-block " href="./update">Sửa</a></td>
    <td><a class="btn-primary btn btn-block " href="./delete/{{_id}}"
        onclick="return confirm('Bạn có muốn xóa sinh viên đó không ?');">Xóa</a></td>
  </tr>
  {{/each}}
</table>